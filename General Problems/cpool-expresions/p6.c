#include <stdio.h>
void main(void){
  float length , wedith , perimeter;
  printf ("Enter the length:");
  scanf("%f",&length);
  printf ("Enter the wedith:");
  scanf("%f",&wedith);
  perimeter = (length+wedith)*2;
  printf ("the Perimeter is %f ",perimeter);
}
  