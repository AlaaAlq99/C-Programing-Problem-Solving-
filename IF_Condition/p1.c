#include <stdio.h>
void main(void){
  int x;
    printf ("Enter the numder  :");
    scanf("%d",&x);
	if (x>0)
		printf ("The num is positive ");
	if (x<0)
		printf ("The num is negative ");
}